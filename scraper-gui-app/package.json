{
  "name": "scraper-gui-app",
  "version": "1.0.0",
  "description": "A simple scraping GUI app",
  "main": "index.js",
  "scripts": {
    "start": "live-server --port=3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["scraper", "frontend", "gui"],
  "license": "ISC",
  "dependencies": {
    "live-server": "^1.2.2"
  }
}
